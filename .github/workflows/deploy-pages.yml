name: Deploy to GitHub Pages

# Note: GitHub Pages serves static files only. The dashboard requires the Express
# API server for live read-only Jira access. For production, run `npm run dev`
# or deploy the full stack (Express + Vite build behind a reverse proxy).

on:
  push:
    branches: [main, master]
    paths:
      - 'jira-dashboard/**'
      - '.github/workflows/deploy-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: jira-dashboard/package-lock.json

      - name: Install & build dashboard
        working-directory: jira-dashboard
        env:
          VITE_BASE_PATH: /QA_Bug_DashBoard/
        run: |
          npm ci
          npm run build
          cp dist/index.html dist/404.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: jira-dashboard/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
